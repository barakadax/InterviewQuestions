# Unlockable Skills

You are given a skill tree where each skill (except the starting skill) has exactly one prerequisite.

The array T represents these dependencies, where T[i] is the prerequisite for skill i.
Given a list of 'target skills' A that a player wants to learn, calculate the minimum total number of skills the player must acquire to meet all prerequisites and reach those targets.
