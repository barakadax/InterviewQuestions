# Vampire Numbers

Need to validate if a number digit is even in its length to split it into equal length fangs,
The multiplication of those fangs should equal the original vampire number,
To avoid trivial cases make sure both numbers doesn't end with zeros.
