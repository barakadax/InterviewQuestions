# Flattening multi-level linked list

Given head for linked list each node contains next and bottom pointer,
Just like a 2D array / matrix,
You're tasked with flattening it into 1D array and keep it in order,
Assume data type int,
Assume all nodes can have next and bottom and it can be circular,
Elements in result should be unique.
